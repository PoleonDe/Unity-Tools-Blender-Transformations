# Blender Transformations Basic Usage

Import this sample for a short note on the editor shortcuts and project settings location.

Open `Project Settings > Control Tools > Blender Transformations` to configure the workflow, then use the Scene View shortcuts from the package.
